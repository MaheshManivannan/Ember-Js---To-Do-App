{{page-title "Todos"}}

<div>
<h1>Todos</h1>
<input {{on "input" this.updateNewTodoTitle}} value={{this.newTodoTitle}} type="text" placeholder="Enter here">
<button {{on "click" this.addTodo}}>Add</button>
<ul>
    {{#each this.todos as |todo|}}
    <li>{{todo}}</li>
    {{/each}}
</ul>
</div>